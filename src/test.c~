#include <stdio.h>
#include <stdlib.h>
using namespace std;
int main(int argc, char * argv[]){
	char * st = "bonjour tout le monde lol";
	int i=0;
	while(st[i]!='\0'){
		cout << st[i] << endl;
		i++;
	}
	return EXIT_SUCCESS;
}
